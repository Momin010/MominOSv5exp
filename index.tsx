import React from 'react';
import ReactDOM from 'react-dom/client';
import MominOS from './components/MominOS';

const root = ReactDOM.createRoot(document.getElementById('root') as HTMLElement);
root.render(
    <React.StrictMode>
        <MominOS />
    </React.StrictMode>
);
