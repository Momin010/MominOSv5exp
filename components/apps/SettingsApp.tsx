import React from 'react';
import { styles } from '../../styles/styles';

const SettingsApp = () => {
    return (
        <div style={styles.appContainer}>
            <h2>Settings</h2>
            <p>System settings and preferences will be available here in a future update.</p>
        </div>
    );
};

export default SettingsApp;
